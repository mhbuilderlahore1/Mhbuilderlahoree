export interface Button {
  id: number;
  title: string;
  link: string;
}

export interface Page {
  id: number;
  title: string;
  slug: string;
}

export interface Post {
  id: number;
  title: string;
  slug: string;
}

export interface Category {
  id: number;
  name: string;
}
